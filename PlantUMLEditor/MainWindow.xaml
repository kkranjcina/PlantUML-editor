<Window x:Class="PlantUMLEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlantUMLEditor"
        mc:Ignorable="d"
        Title="PlantUML Editor" Height="720" Width="1280" WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <!-- Prostor za prikaz dijagrama -->
        <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1">
            <Canvas Name="DiagramCanvas" Background="White"/>
        </Border>

        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      Background="Gray" Width="5"/>

        <!-- Bočni panel za odabir načina rada -->
        <StackPanel Grid.Column="2" Margin="10">
            <TabControl>
                <TabItem Header="Ručno kreiranje">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBox Name="PlantUmlCodeTextBox" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 Grid.Row="0" Margin="0,0,0,10" />

                        <Button Content="Generiraj dijagram" Grid.Row="1" Click="GenerateDiagram_Click"
                HorizontalAlignment="Right" Width="120" />
                    </Grid>
                </TabItem>

                <TabItem Header="ChatGPT">
                    <Grid>
                        <StackPanel Margin="10">
                            <TextBlock Text="Upit za ChatGPT:" Margin="0,0,0,5"/>
                            <TextBox x:Name="ChatInputTextBox" Height="100" TextWrapping="Wrap"/>

                            <Button Content="Pošalji upit ChatGPT-u" Margin="0,10,0,0" Click="SendChatGPTQuery_Click"/>

                            <TextBlock Text="Odgovor ChatGPT-a:" Margin="0,20,0,5" FontWeight="Bold"/>
                            <TextBox x:Name="ChatResponseTextBox" Height="150" TextWrapping="Wrap" IsReadOnly="True"/>
                        </StackPanel>
                    </Grid>
                </TabItem>
            </TabControl>
        </StackPanel>

        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ShowsPreview="True"/>
    </Grid>
</Window>
